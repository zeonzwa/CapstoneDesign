<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.capstone.mappers.memberMapper">

<insert id="signup">
 insert into member(name, id, pw, major, student_num, email, phone_number)
  values(#{name}, #{id}, #{pw}, #{major}, #{student_num}, #{email}, #{phone_number}) 
</insert>

<!-- 로그인 -->
<select id="signin" resultType="com.capstone.domain.MemberVO">
	select
	    name, id, pw, major, student_num, email, phone_number
	from
	    member
	where id = #{id}
</select>

</mapper>