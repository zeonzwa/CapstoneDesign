<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.capstone.mapper.MailMapper">
  	
  	<!-- 아이디 중복검사 -->
  	<select id="idCheck" resultType="int">
  		select count(*) from member where Id = #{Id}
  	</select>
  	
  	<!-- 회원 가입 -->
	<insert id="memberJoin">
 		insert into member (Name, Id, Pw, Major, Student_Num, Email, Phone_Num, RegiDate, Verify, tal_Sta, goods_Sta)
  			values(#{Name}, #{Id}, #{Pw}, #{Major}, #{Student_Num}, #{Email}, #{Phone_Num}, sysdate, 0, 0, 0) 
	</insert>

  </mapper>